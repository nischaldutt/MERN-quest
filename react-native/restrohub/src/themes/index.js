import { DefaultTheme } from "react-native-paper";

const restrohubTheme = {
  ...DefaultTheme,
  roundness: 2,
  colors: {
    ...DefaultTheme.colors,
    primary: "#325842",
    accent: "#fff",
    backdrop: "#999",
  },
};

export default restrohubTheme;
